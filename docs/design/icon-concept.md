# HEY Agent icon concept

## Revision 2 — Tokyo Night / Omarchy maze

[Selected icon](hey-agent-icon-concept-v2.png), generated with the built-in image-generation tool on 2026-09-04. Uses the official [Omarchy brand mark](https://omarchy.org/brand/) as its main geometry reference, Tokyo Night lavender `#bb9af7` on navy `#1a1b26`, a filled inset mail square, and a bottom edge without the rejected leg. Applied to `resources/icon.png`, the AppImage, and the desktop installer. The launcher uses the installed PNG directly so an old themed SVG cannot override it.

Prompt:

> Use case: logo-brand. Revise the HEY Agent icon. Image 1 is the REQUIRED GEOMETRY reference: official Omarchy mark with a filled inner square and a concentric broken square maze-like outer channel. Image 2 is the REJECTED prior concept: do NOT preserve its empty envelope-only frame, blue color, rounded V tip or bottom leg. Make the icon look clearly like image 1 with its distinctive square-in-square labyrinth: preserve the inset large solid square, surrounding angular path, gaps at top middle and left middle and bottom middle. Clip the bottom protruding stem flush to the outer square bottom edge: absolutely NO leg, tail or stroke outside square bounds. Integrate an envelope INSIDE the filled inner square by cutting a crisp dark negative-space V-fold into its upper half, and minimal diagonal fold seams if needed. The inner square IS the mail envelope body; do not remove it. Keep outer maze paths rectangular and stepped at 90 degrees, strong even widths. Flat Tokyo Night palette: filled logo lavender #bb9af7, backdrop and cutout channels dark navy #1a1b26. NO saturated blue, green, gradients, glow, shadows, checkerboard, text or badges. Square single icon concept, centered with even generous padding on a solid dark navy background. One mark only. Precision geometric flat vector-like graphic, legible at desktop icon size.

## Revision 1 — rejected

Rejected preview: [Omarchy mail concept](hey-agent-icon-concept.png). Generated with the built-in image-generation tool on 2026-09-04. Superseded by the selected revision above; never used as the packaged icon.

The generator rendered the transparency checkerboard into the RGB image rather than producing an alpha channel. This file is a visual concept only, not an installable icon export.

References: the user's supplied square blue mail mark, and the locally installed official Omarchy icon from [basecamp/omarchy](https://github.com/basecamp/omarchy/blob/master/icon.png). This is a combined concept, not a claim of official affiliation.

Generation prompt:

> Use case: logo-brand. Create a single clean desktop application icon concept for HEY Agent, no text. Input 1 is a visual reference for a square mail/envelope mark with a bold descending V flap. Input 2 is the official Omarchy icon reference: use its distinctive angular broken-square / offset bracket silhouette as the actual outer envelope, NOT a conventional rounded square and NOT a separate decorative badge. Fuse the two ideas into ONE coherent mark: the outer frame recognizably has Omarchy's squared corners, thick strokes, intentional breaks and stepped edges, and the inner negative space contains a strong simple mail V flap. Crisp flat vector-like geometry. One monochrome blue mark close to the first reference blue, on genuinely transparent background. Centered square composition with generous even 12 percent padding, legible at 32px. No gradients, no shadows, no glow, no extra arrows, no lettering, no mockup, no multiple variants. This is a concept preview, do not add any text or labels.
